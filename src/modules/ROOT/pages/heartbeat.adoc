= Heartbeat

// TODO: https://martinfowler.com/articles/patterns-of-distributed-systems/heartbeat.html
// TODO: https://blog.algomaster.io/p/heartbeats-in-distributed-systems

In *xref:distributed-system.adoc[distributed systems]*, a heartbeat is a
*periodic message* sent between nodes to monitor each other's health and status.

Without a heartbeat mechanism, it's hard to quickly detect failures in a
distributed system, leading to:

* Delayed fault detection and recovery.
* Increased *xref:downtime.adoc[downtime]* and errors.
* Decreased overall system reliability.

A missing heartbeat is itself a signal. *xref:alerting.adoc[Alerting]* systems
can raise a dead man's switch alert when heartbeats stop arriving, catching
total failures that value-based thresholds would miss.

Heartbeat state can also be propagated through a
xref:gossip-protocol.adoc[*gossip protocol*], so that every node eventually
learns which peers have stopped heartbeating and can treat them as failed.
