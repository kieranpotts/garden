# Regular expressions

Regular expressions ("regex") are used to find patterns in text. They use special syntax and concepts to obtain information from a string.

To generalize a bit, regex are used for two things: to validate data, and to extract information from data. Most programming languages have some sort of support for regex, because they're so useful for these purposes.
